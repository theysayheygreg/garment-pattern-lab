# Changelog

## 2026-05-03

- Created initial LBH-style project structure.
- Added README and agent notes.
- Added product plan.
- Added first-garment spec.
- Added roadmap to first prototype.
- Added build plan, project board, and project state file.
- Added dependency map and research queue.
- Added reference bibliography.
- Added UV-to-pattern architecture note.
- Added software landscape and fundamentals notes.
- Added initial decision log and devlog.
- Added local full PDF copy of `Computational Pattern Making from 3D Garment Models` from arXiv.
- Added deep paper ingest note for `Computational Pattern Making from 3D Garment Models`.
- Added `docs/project/KNOWLEDGE-GRAPH.md`.
- Updated product plan, roadmap, dependencies, research queue, build plan, project board, backlog, UV notes, fundamentals, decision log, and devlog with sewing-aware validation concepts from the paper.
- Added deep ingest notes for patternmaking fundamentals, commercial software, open/free tools, UV geometry workflows, and remaining research papers.
- Expanded the product knowledge graph with secondary-ingest nodes and edges for pattern programs, parametric tools, industrialization metadata, UV boundaries, datasets, multimodal generation, and ML representations.
- Added local Graphite and Blender setup notes plus a pipeline plan for 2D sketch, 3D preview/render, and pattern flats.
- Added browser-native Three.js/WebGPU/WASM pipeline plan for an ownable 2D reference -> 3D model -> flats -> pattern product lane.
- Added AI sketch and 3D exploration lanes for GPT Image 2 sketch corpus generation, modern image-to-3D candidate frameworks, and visual-corpus evaluation.
- Refined the third AI exploration lane into a garment-type pattern-reference corpus for construction correctness examples.
- Added candidate-to-export interop layer for measuring, correcting, validating, and round-tripping pattern candidates before SVG/PDF/DXF export.
- Expanded interop with canonical millimeter units, scale proof, and marker/fabric-roll layout planning.
- Added a consolidated deep-dive synthesis for candidate proof, browser geometry kernels, marker optimization, visual corpus truth levels, GPT Image 2 sketch generation, and image-to-3D candidate frameworks.
- Expanded the product knowledge graph with `PatternKernel`, `GeometryKernel`, WASM/WebGPU runtime nodes, marker optimizer metrics, visual corpus truth/licensing nodes, image-to-3D mesh quality nodes, and SVG/DXF semantic round-trip fixtures.
- Updated the research queue and project state with the next concrete spikes: pattern kernel contracts, visual corpus schema, marker optimizer interface, SVG semantic profile, and SPAR3D/Hunyuan comparison.
- Promoted the twelve remaining research areas into a formal research roadmap inside `docs/project/ROADMAP.md`.
- Added a thirteenth research lane for designer sketch-to-model editing, including raster/vector sketch layers, surface projections, PBR-style material preview layers, edit classification, and live model feedback.
- Refined the designer editing lane into a smaller v1: edit concrete sketch features such as shoulder opening, armhole, neckline, side silhouette, hem length, and hem sweep, then map those edits to garment parameters before regenerating pattern/model feedback.
- Started executing the research roadmap with a dependency/prior-art pass across drafting, schema, validation, geometry, SVG, marker planning, visual corpus, image-to-3D, commercial interop, and designer editing.
- Added `DEPENDENCY-REGISTER.md`, `EXAMPLE-NEEDS.md`, and `roadmap-research-pass-2026-05-03.md`.
- Expanded references with FreeSewing core/dev docs, GarmentCode repo/specs, browser geometry libraries, marker/nesting libraries, visual corpus sources, image-to-3D frameworks, and designer/PBR prior art.
- Split output strategy into a v1 human-readable pattern package lane and a later machine-readable cutter/CAD lane.
- Added a high-level architecture overview and ported the core pipeline/tooling summary into the README.
- Added architecture maintenance policy, `THINGS-TRIED.md`, README links, and a decision-log entry for preserving explored options while keeping the current architecture overview focused.
- Added CAD-to-technical-drawing reference pass, then separated it from the sewing-first product architecture with `PatternPackageModel`, `PatternPackageComposer`, and `SewingPatternSheetProfile`.
- Added a dedicated pattern standards and conventions reference, separating common sewing-pattern notation from ASTM/ISO measurement, seam, stitch, and DXF exchange standards.
- Clarified that ASTM/ISO/AAMA references are backstage interop and schema material, while the v1 user-facing output should stay sewing-native for designers, small shops, sample makers, and sewers.
- Reframed industry-standard ASTM/AAMA/DXF output as a later export option alongside machine-readable cutter/CAD output, not a v1 product surface.
- Added first-garment reference corpora for the two requested lanes: sketch/design visual references and real pattern/construction references.
- Added corpus methodology: run one first-garment exemplar through the full pipeline, but validate family behavior against a 5-10 item same-garment variation set.
- Clarified corpus rights policy: copyrighted public patterns are allowed as reference-only evaluation material, while GPT Image 2/project-owned sketches and authored PatternGraph fixtures provide original pipeline inputs.
- Split sketch input into two lanes: GPT Image 2 generated fixtures for prompt-controlled original examples, and human-authored drawings/vectors/uploads for the designer-facing ingestion path.
- Added product design framing: Garment Pattern Lab is a sketch-to-pattern workbench for fashion designers, not just an AI garment pipeline.
- Added Kiko/Kew competitor shortlist as adjacent reference context beside the product design brief.
- Added Kew competitor deep dive and updated the knowledge base with competitor capability and Optitex pillar framing.
- Promoted the core product differentiator: not another mouse-and-keyboard CAD/3D editor, but a human-centered, natural-language-led, task-led path from art to garment.
- Added Kew sample image analysis and knowledge-graph nodes for reference images, croquis guides, measurement overlays, technical callouts, and ambiguity questions.
- Clarified that Kew-style vector/layer editing is a useful pipeline bridge and that Pattern Lab's added value is the missing pattern/validation/3D feedback loop.
- Added product pillars and prototype build order docs.
- Scaffolded `app/`, reusable `packages/`, and `garments/a-line-dress-tunic/` so core product code stays separate from garment-specific programs and fixtures.
- Expanded the roadmap with pipeline package owners, first-garment folder paths, and a granular validation-first B0-B10 build sequence.
- Added Onshape deep-dive reference for future multi-user collaboration, PDM, PLM, MBD/PMI, release/version discipline, and high-end rendering.
- Added the ideal outcome to product design: being a good clothing designer should not require CAD expertise.
- Added Orrery design review (`docs/research/orrery-design-review-2026-05-03.md`) covering product direction, lower-level design, and roadmap with 18 structural findings before implementation begins. Two findings already aligned with Greg: candidate-promotion gate as a state machine, and v0.1 as a dirty end-to-end spike on one verifiable garment.
- Clarified validation product direction: backend interpretation/readiness instrumentation, not an IDE/game-engine/3D-tool error console or user-blaming repair flow.
- Added Codex response to the Orrery review and a v0.1 dirty end-to-end spike plan as the next implementation move.
- Added the first v0.1 dirty end-to-end package: synthetic measurements, garment parameters, ugly generator, PatternGraph candidate, readiness report, SVG pattern, cut sheet, assembly notes, static preview, and human sanity-check scaffold.
- Ingested Kiko's Kew Product Vision (`docs/reference/KEW-PRODUCT-VISION.md`) alongside the existing Kew artifacts. Captures Kew's full ecosystem-scale north star (idea-to-finished-product, garment-as-living-product-object, AI-native fashion engine, beyond-SaaS framing), the five named product families (Canvas/CAD/PD/Lifecycle/Studio), the drape-to-pattern workflow as a third input lane, the long-range compatibility targets (Adobe/Gerber/CLO/Ned Graphics/Lectra/Browzwear/Optitex), and the strategic position that Garment Pattern Lab is the focused execution slice for the Kew PD layer. Surfaces a clean answer to the canvas question: the canvas lives upstream, Pattern Lab receives a `ConfirmedIntentPayload`.
- Expanded the product knowledge graph with a Kew Product Vision Layer: `KewProductVision`, `LivingProductObject`, `TranslationProblem`, `KewCanvasFamily`, `KewCADFamily`, `KewPDFamily`, `KewLifecycleFamily`, `KewStudioFamily`, `DrapeToPatternWorkflow`, `DrapePhotoInputLane`, `GuidedProductIntelligence`, `ExpertNoviceContinuum`, `OpenSourceLearningSpirit`, `IntelligenceLearningLoop`, `AINativeFashionEngine`, `KewMoatBoundary`, `KewCompatibilityTarget`, `MultilingualFactoryInstruction`, `CrossDomainAbstractionConstraint`, `KewUserSpan`, `AvatarToRealGarmentBridge`, `ConfirmedIntentPayload`, and `KewGPLAlignmentPlane`. Added matching edges and four representation boundary rules covering the GPL-as-Kew-PD-slice stance, upstream canvas ownership, and label-registry multilingual handling.
- Extracted first reusable engine modules from the v0.1 generator: `pattern-core` measurement helpers, `validation-core` readiness instrumentation, and `export-core` package builders. Added a dependency-free readiness smoke test and expanded `npm run check` so bad units and mismatched side seams prove they block readiness.
- Added the first task-led designer edit lane: `lengthen hem 100mm` is interpreted by `assistant-core`, applied as a structured parameter edit, and regenerated into `garments/a-line-dress-tunic/outputs/v0.1-length-plus-100/` with `edit-intent.json` and `edit-summary.md`.
- Added a static app workbench generated by `npm run app:build` at `app/dist/workbench.html`, comparing the base v0.1 package with the `lengthen hem 100mm` package and linking to their SVGs, previews, and edit summary.
- Phase A implementation cleanup: split generated outputs into `package/` and `dev-artifacts/`, moved internal comparison HTML to `app/dev-artifacts/dev-comparison.html`, and reclassified `lengthen hem 100mm` as a dev/v0.5 seed rather than a v0.1 acceptance feature.
- Started Phase B with a dependency-free `sketch-intent` ingest bridge: recipe definitions, SVG passthrough, `InputProvenance` hashing, deterministic trace-layer buckets (`silhouette`, `interior`, `annotation`, `unclassified`), and a clean technical-flat smoke fixture wired into `npm run check`.
- Advanced Phase B with a runnable raster smoke path: installed `@neplex/vectorizer`, traced synthetic PNG input through a VTracer-backed Node bridge, emitted the same `EditableTraceLayer` contract as SVG passthrough, and documented Potrace as research/isolated comparison rather than the default prototype dependency because of GPLv2.
- Added Phase B vector document smoke coverage: vector PDF and PDF-compatible `.ai` files convert through local Poppler `pdftocairo` to SVG, then feed the same editable trace-layer classifier. `check:sketch` now generates synthetic PNG fixtures for all four recipes plus vector PDF / `.ai` fixtures.
- Hardened Phase B SVG fixture handling: the bridge now normalizes common SVG primitives (`polygon`, `polyline`, `line`, `rect`) into trace records, added a primitive-export fixture, added backend readiness status for fixture work, and added `npm run sketch:report` for local ingest summaries.
- Corrected the Kew Product Vision ingest after Greg pushback: (1) Pattern Lab owns a narrow interpretation/correction surface for incoming art, not a freeform vector-authoring tool; the canvas is not strictly upstream. Replaced `ConfirmedIntentPayload` with `SketchIntentPromotionState` (`interpreted` → `user-corrected` → `confirmed`) and added `SemanticInterpretationSurface`, `VectorIngestProfile`, `RasterToVectorBridge`. (2) Drape photo is a content variant inside the existing human-authored lane plus a vectorization step — not a third input lane. (3) Kew compatibility list is a *replacement* target, not a round-trip export target; renamed `KewCompatibilityTarget` to `KewReplacementTarget` and added `KewIngestSurface` for one-way input from `.ai`/`.svg`/vector-PDF/raster. Updated boundary rules and edges in `KNOWLEDGE-GRAPH.md` to match. Added a sharpened prototype goal to `KEW-PRODUCT-VISION.md`: "produce a pattern from a sketch or image reference that a human like Kiko could actually sew."
- Stop-time review (Codex) caught residual stale guidance in `KEW-PRODUCT-VISION.md` after the corrections landed. Rewrote the upstream sections that still carried the old framing so the doc is internally consistent end-to-end: "The Drape-To-Pattern Workflow" no longer calls drape a third input lane, "Real-World Compatibility Targets" splits the systems list into replacement aim / ingest worth supporting / no round-trip rather than treating them as future export targets, the Kew PD mapping clarifies that Pattern Lab owns its own interpretation/correction surface (not Kew CAD's freeform authoring), and the open-questions section reframes the upstream-payload question to match the corrected interpretation-inside-Pattern-Lab model.
- Spun up three background research subagents covering (a) garment design craft prior art and taxonomy, (b) vectorization SOA for garment-aware ingestion, (c) semantic curve interpretation SOA. Output is split per lane between reference docs in `docs/research/` and structured corpus files in `docs/data-corpus/`. The vectorization lane has landed: `docs/research/vectorization-for-garment-ingestion-2026-05-03.md`, `docs/data-corpus/vectorization-approaches.json`, `docs/data-corpus/README.md`. Headline recommendation: Potrace (`esm-potrace-wasm`) + VTracer (Rust+WASM, MIT) behind a recipe-driven `RasterToVectorBridge`, with `.svg`/`.ai`/vector-PDF passthrough as the highest-trust path. Potrace GPLv2 flagged for `DEPENDENCY-REGISTER.md` follow-up.
- Decision: garment language is enforced as the system-wide voice (`docs/journal/DECISION-LOG.md`). Binds in three places — schema (every user-facing field has paired voice phrasing), tooling (CI linting prevents jargon leaks), and corpus (language phrasings are first-class data records). Settles Orrery design-review finding 13 and Pillar 1 in commit form, not stylistic-guideline form.
- Corrected the garment-language decision after Greg pushback: the binding is the **user surface only**, not the whole system. The earlier framing accidentally re-imported the IDE/error-console mental model by listing validator findings, fix suggestions, and ambiguity questions as user-facing. They are not — they are engine instrumentation and live on the dev surface in engineer language. The user surface is three things: the workbench UI, the assistant collaborator, and the pattern package output. Garment language binds in those three only. Translation from engine instrumentation to user surface happens at the boundary and only for the small subset of engine state actually relevant to the user. Decision-log entry rewritten accordingly.
- Semantic curve interpretation subagent landed: `docs/research/semantic-curve-interpretation-soa-2026-05-03.md`, `docs/data-corpus/semantic-interpretation-methods.json` (33 records), `docs/data-corpus/garment-family-landmark-priors.json` (14 landmark slots for the v0.1 garment with weighted heuristic rules). Headline recommendation: heuristic-first interpreter loading per-garment-family priors with weighted geometric rules, low-confidence escalation to a predict→present→accept-or-correct surface, every correction logged as labeled training data. Progressive enrichment lanes (gradient-boosted classifier → SketchGNN → multimodal) all share one swappable `CurveRoleAssignment` output schema, so heuristic and ML interpreters are interchangeable rather than parallel.
- Stop-time review (Codex) flagged that `docs/data-corpus/garment-family-landmark-priors.json` is not implementable as documented. Added an explicit `known_implementability_gaps` section to the file naming the five concrete gaps (mirror rules referenced by id without bodies, prose rule strings rather than DSL, undocumented labeling-order dependencies, unit-bearing priors on unitless input, missing axis-absent fallback). Each gap includes a proposed resolution. The file remains a useful design-time prior catalog; the gaps are now honest deferrals to the implementation lane rather than hidden contradictions.
- Garment craft prior art subagent landed: `docs/research/garment-craft-prior-art-2026-05-03.md`, `docs/data-corpus/garment-families.json` (22 families across dresses/tops/skirts/pants/jackets), `docs/data-corpus/craft-conventions.json` (6 notch types, 9 line types, label phrasing, regional sizing for US/EU/UK/JP, 7 block archetypes plus 4 regional drafting traditions), `docs/data-corpus/drafting-formulas-a-line-tunic.json` (hybrid: FreeSewing Bella structural factors with `factor_provenance` citations, Aldrich-style absolute-cm ease defaults, SewGuide A-line flare math, full back/front panel steps with optional bust dart via apex rotation, 4 neckline shapes, 6 v0.1 validation rules with `fix_suggestions`). Open question for the patternmaker checkpoint: hybrid drafting math and ease defaults are unaudited.
- Spun up a fourth research subagent (in background) to investigate small bespoke models and RL techniques for two state-machine roles where the validation gate can serve as a deterministic verifier: (a) the vectorization + semantic interpretation state machine, and (b) the pattern refinement state machine. Briefed on the DAG spine, the bounded-domain reality (~50 landmark types, ~12 commands, ~10 garment families), browser-deployable preference (WASM/ONNX/WebGPU, < 200ms interactive, < 50ms inner refinement loop), and staged enrichment expectations (v0.2 → v0.5 → v1).
- Decision: `PatternGraph` is a graph, not an Illustrator layer tree (`docs/journal/DECISION-LOG.md`). Panels, edges, modifiers (darts/pleats/notches), allowances, markers (grainlines/folds), anchors, and seam pairs are all first-class typed nodes. Edges between them are semantic and typed (`belongs_to`, `attaches_to`, `paired_with`, `derived_from`, `anchors_at`). Cross-panel relationships are first-class edges, not tree crawls. Three orthogonal axes layer onto the graph: state (candidate/promoted), revision (operation-DAG provenance), provenance (producer/source/confidence). Settles question 1 of the schema-spine pass; node-type and edge-type catalogs and the JSON schema remain open until B1's seed fixture makes them concrete.
- Bespoke-model subagent landed: `docs/research/bespoke-model-opportunities-2026-05-03.md`, `docs/data-corpus/bespoke-model-candidates.json` (45 candidates with `known_gaps` register), `docs/data-corpus/README.md` updated. Headline V-Model: Set Transformer (~1.5M params) on frozen MobileSAM ViT-Tiny encoder, per-family LoRA, ONNX Runtime Web; classical tracers propose curves, model assigns roles. Headline R-Model: SmolLM2-135M or Qwen2.5-0.5B with per-family QLoRA at 4-bit GGUF (~70MB) via WebLLM/MLC, AlphaFold-shape iterative refinement loop, validation gate as scorer. Both share the DAG spine via provenance-tagged operations; heuristic remains the floor. Staged v0.2 → v0.5 → v1.
- Wrote three canonical user personas at `docs/design/personas/`: Persona 1 (Individual Fashion Designer, v0.1 primary user), Persona 2 (Production-Focused Garment Designer, v0.5+), Persona 3 (Manufacturing-Focused Designer, v1+). Each persona includes identity, context, quality bar, what they see / don't see, version target, 8–10 user stories, anti-stories, voice/dialect, and open questions. Personas are canonical scope-defining tools, not marketing avatars.
- Plugged personas into the broader design docs: `PRODUCT-DESIGN.md` now opens with the three-persona framing and the v0.1 primary-user commitment to Persona 1; `PRODUCT-PILLARS.md` adds a pillar-to-persona mapping table showing how each pillar expresses differently across personas/versions; `PRODUCT-PLAN.md` user section reframed as persona references rather than a flat list; adjacent users (costume designers, game/film modelers, etc.) noted as edge cases against the canonical three.
- Decision: three canonical personas anchor scope by version (`docs/journal/DECISION-LOG.md`). Persona 1 = v0.1 primary, Persona 2 = v0.5+, Persona 3 = v1+. Every feature audit becomes a one-question check: "which persona, which version?" Persona docs are canonical; other docs reference rather than restate.
- Audited existing v0.1 work through the persona lens: `docs/research/persona-audit-of-existing-work-2026-05-03.md`. Findings: cut sheet and assembly notes voice are well-aligned with Persona 1; the workbench HTML at `app/dist/workbench.html` is wrong-personaed (it's a Persona 2 variant-comparison view, not a Persona 1 single-pattern editor); the user-package and dev-package mix `readiness.md` (internal instrumentation) into the same directory as user artifacts (`cut-sheet.md`, `pattern.svg`) — should be split. Six-item closing punch list named for v0.1 to actually meet Persona 1's product promise: split user-package from dev-artifacts, build a Persona-1-shaped workbench, expand assistant verbs to 5–6, implement the sketch-input lane via the V-Model baseline, add tiled-PDF export, build or honestly relabel the 3D preview.
- Refined the persona audit after Greg pushback: (1) the workbench HTML is reclassified as a dev artifact, not a wrong-personaed user surface — v0.1 and v0.5 are headless code-harness territory, the visual side-by-side (vector + 3D) is a v1 magic moment. Renamed/relocated as a follow-on dev task. (2) The user-package vs dev-artifacts split is treated as a developer task (Codex / dev), not a user-research item. (3) The "sketch input lane is empty" finding consolidated into the existing V-Model + vectorization + semantic-interpretation + interop research lanes already designed; not a fresh finding. v0.1 closing punch list shrinks from six items to five, all dev-shaped: package split, expand assistant verbs (5–6), implement V-Model baseline sketch lane, tiled-PDF export, honest 3D preview labeling or B6 build.
- Wrote eight grounded Persona 1 example flows at `docs/design/personas/persona-1-example-flows.md`. Each flow is anchored to a specific garment family from the freshly-landed `docs/data-corpus/garment-families.json` (a-line dress, sheath/bodice, tee, shift, slip, drawstring trouser, gathered skirt, wrap dress) and a real input mode (Instagram reference, drape photo, physical-garment trace, Procreate vector export, Kew-style technical flat, napkin sketch, Pinterest photo, sketch + reference combo). Flows show common patterns (own input + own measurements, conversational edits in design language, honest-about-limits, printable output) and explicitly enumerate which v0.1 capabilities they exercise and which v0.5+ / v1+ capabilities they correctly don't reach. Linked from `persona-1-individual-designer.md`.
- Wrote canonical v0.1 design spec at `docs/project/V0.1-DESIGN.md`. v0.1 is a one-shot pipeline with a thin Three.js viewer + export button: clean image/sketch/vector input → Potrace+VTracer vectorization → heuristic semantic interpretation → figure-driven imperial scale calibration → A-line tunic drafting (FreeSewing+Aldrich+SewGuide formulas) → validation → simple 45" marker layout → tiled-PDF export package. Garment family locked to the sleeveless A-line woven tunic. No editing in v0.1, no manual correction surface, no variants/grading. Failure mode: best-guess output with assumptions surfaced in the package; hard refusal only below confidence floor. Spec includes ten stages, ten implementation phases (A–J) for Codex with done-when criteria per phase, explicit acceptance criteria, and explicit non-goals. Resolves all five `known_implementability_gaps` in the landmark prior file as Phase C work.
- Reflected the v0.1 design across project docs: `BUILD-PLAN.md` current milestone updated to "M0.5 v0.1 Design Locked, Implementation Ready" with all M0 items marked complete and v0.1 implementation phases marked open; `PROJECT-STATE.json` status updated to "v0.1-design-locked-implementation-ready" with persona references and v0.1 design path; `README.md` Current State and Working Docs updated to lead with the v0.1 design spec.
- Decision: v0.1 design locked (`docs/journal/DECISION-LOG.md`). One-shot pipeline + thin Three.js viewer + 45" marker + imperial + A-line tunic. The 3D viewer is static panel placement, not cloth simulation; explicitly labeled as such to prevent the games-industry "drape" terminology confusion. The viewer is the seed of the v1 magic-moment side-by-side; v0.5 adds editing.
- Final stop-time pass to ensure today's open questions are durable, not in agent memory: (1) added the drape vocabulary note to `docs/design/personas/persona-1-example-flows.md` distinguishing fabric-property drape (a), patternmaking-by-drape (b.1, our v0.5 input lane), tailoring-by-drape (b.2, future Persona 3 territory), and cloth-simulation drape (games/3D, explicitly not v0.1/v0.5); (2) added a `fabric-properties.json` placeholder section to `docs/data-corpus/README.md` naming the future `drape_behavior` field on fabric records; (3) extended the graph-not-tree decision-log entry with the architectural references (B-rep / Onshape / BMesh as gold reference, ECS adjacent but wrong fit, GarmentCode adopted with four explicit extensions: first-class Allowance / Anchor / Modifier / ordered Construction; commercial formats as proprietary blobs not data-model references; DXF/AAMA/ASTM as future export target not internal model).

## 2026-05-04

- Codex Phase A complete: existing v0.1 outputs split into `package/` and `dev-artifacts/`, dev comparison page moved to `app/dev-artifacts/dev-comparison.html`. `BUILD-PLAN.md` and `PROJECT-STATE.json` updated to reflect M0.6 status.
- Three v0.1 implementation-blocking decisions answered (`docs/journal/DECISION-LOG.md`):
  (1) Canonical body fallback when input lacks a human figure is **US Misses 8** (ASTM D5585 or comparable), stored in `garments/a-line-dress-tunic/fixtures/measurements/canonical-misses-8.json`. Selectability is a v0.1.x or v0.5 priority.
  (2) Reference inputs for the Phase J smoke test come from **web-clean reference sketches plus GPT Image 2 generation** per Lane A in `INPUT-LANES.md`, with the eight Persona 1 example flows as prompt targets. Unblocks Codex without waiting on Kiko-supplied inputs.
  (3) `.ai` ingestion uses a **known library** (recommended: `pdfjs-dist` for PDF-compatible `.ai` files; Codex selects during Phase B). Pre-CS2 `.ai` files fall back to a clear error.
- `V0.1-DESIGN.md` Open Questions section reorganized into "Resolved 2026-05-04" and "Still open (workable around during implementation)" buckets. Several previously-open items also given explicit defaults: landmark vs measurement precedence (measurements scale, landmarks position), hard-refuse floor (0.3 default), asymmetric inputs (refuse), failure-case viewer behavior (best-guess overlay + guidance page).
- Wrote user correction surface design sketch at `docs/design/user-correction-surface.md`. v0.5 design exploration (not v0.1 scope). Five operations (correct / remove / add / confirm / adjust), bounded by the garment-family landmark vocabulary, each operation a DAG node with provenance feeding the V-Model training corpus. Key framing: this surface is the most-used in early versions and the least-used in late versions — the V-Model trains on user corrections, so each release should ask for fewer corrections than the last. Without this surface, the V-Model can't progress past v0.2's synthetic-only training. Distinguishes interpretation correction (Persona 1, fix what the system saw) from parameter editing (Persona 2, change garment design choices on a confirmed pattern).
- Codex updated V0.1-DESIGN Stage 2 vectorization engine: swapped Potrace (GPLv2 license drag) for `@neplex/vectorizer` (MIT VTracer-backed, Buffer input, Node-runnable). Cleaner license story; Potrace remains a research comparison but isn't on the default prototype dependency path. License-clean by default.
