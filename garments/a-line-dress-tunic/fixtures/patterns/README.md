# Pattern Fixtures

`PatternGraphCandidate` and `PatternGraph` fixtures for the first garment.

Start with:

- one valid hand-authored seed
- invalid seam-length fixture
- invalid grainline fixture
- invalid scale/unit fixture
- invalid self-intersection fixture
- invalid allowance/cut-line fixture
