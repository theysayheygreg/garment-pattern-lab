# Geometry Core Source

Future deterministic geometry operations.

Owns:

- curve length
- offsets
- intersections
- closedness and self-intersection checks
- triangulation handoff
- marker-placement primitives

This package should expose stable contracts before the project commits deeply to TypeScript-only, Rust/WASM, C++/Emscripten, or WebGPU implementation details.
