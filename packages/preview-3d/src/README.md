# Preview 3D Source

Future 3D feedback package.

Owns:

- coarse avatar/proxy setup
- panel placement
- seam visualization
- silhouette preview
- rough clearance/ease warnings
- screenshot/export artifacts

The 3D preview is feedback. It must not silently rewrite `PatternGraph`.
