# App Source

Future browser workbench source.

This folder owns orchestration and user-facing workflow only:

- sketch/import surface
- assumption review
- pattern and validation views
- 3D preview viewport
- export package flow

Garment drafting rules belong in `garments/`. Reusable domain logic belongs in `packages/`.
